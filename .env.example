KEYSTATIC_GITHUB_CLIENT_ID=
KEYSTATIC_GITHUB_CLIENT_SECRET=
KEYSTATIC_SECRET=

# Set these in the Cloudflare Pages project settings too, not just locally —
# the Keystatic admin UI needs them on the deployed build. See DEPLOY.md.
